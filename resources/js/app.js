require('./bootstrap');
require('chart.js');

// window.Vue = require('vue');

//const files = require.context('./', true, /\.vue$/i);
//files.keys().map(key => Vue.component(key.split('/').pop().split('.')[0], files(key).default));

// Vue.component('graph', require('./components/Graph.vue').default);
// Vue.component('graphBar', require('./components/GraphBar.vue').default);
// Vue.component('tambahPemeriksa', require('./components/TambahPemeriksa').default);
// const app = new Vue({
//     el: '#wrapper',
// });
